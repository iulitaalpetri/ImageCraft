import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  logoContainer: {
    marginBottom: 30, // Adjust based on your layout
  },
  logo: {
    width: 250, // Set your desired size
    height: 250,
  },
});

export default styles;
